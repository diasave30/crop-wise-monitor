import { Link, createFileRoute } from "@tanstack/react-router";
import { ArrowUpRight, MapPin } from "lucide-react";
import { AppShell, Breadcrumb, PageHeading } from "@/components/layout/app-shell";
import { StatusBadge } from "@/components/shared/status";
import { farms } from "@/data/mock-data";

export const Route = createFileRoute("/farms")({ head: () => ({ meta: [{ title: "Farms — Ceres Intelligence" }, { name: "description", content: "Monitor field health across every farm and zone." }, { property: "og:title", content: "Farms — Ceres Intelligence" }, { property: "og:description", content: "Monitor field health across every farm and zone." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: FarmsPage });

function FarmsPage() { return <AppShell title="Farms"><Breadcrumb label="Farms" /><PageHeading title="Farms" description="A current view of farm health, coverage, and zones requiring attention." /><div className="panel overflow-hidden rounded-lg"><div className="grid grid-cols-[minmax(0,1fr)_auto_auto] gap-4 border-b border-border bg-muted/45 px-5 py-3 text-[10px] font-medium uppercase tracking-[0.14em] text-muted-foreground"><span>Farm</span><span className="hidden sm:block">Coverage</span><span>Health</span></div><div className="divide-y divide-border">{farms.map((farm) => <Link key={farm.id} to="/farms/$farmId" params={{ farmId: farm.id }} className="grid grid-cols-[minmax(0,1fr)_auto_auto] items-center gap-4 px-5 py-5 transition-colors hover:bg-muted/35"><div className="flex min-w-0 items-center gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-md bg-accent text-accent-foreground"><MapPin className="size-4" /></span><div className="min-w-0"><p className="truncate text-sm font-medium">{farm.name}</p><p className="mt-1 truncate text-xs text-muted-foreground">{farm.location} · {farm.cropType}</p></div></div><div className="hidden text-right sm:block"><p className="text-sm font-medium">{farm.zones} zones</p><p className="mt-1 text-xs text-muted-foreground">{farm.attentionZones} need attention</p></div><div className="flex items-center gap-3"><StatusBadge status={farm.overallHealth >= 85 ? "healthy" : "moderate"} /><ArrowUpRight className="size-4 text-muted-foreground" /></div></Link>)}</div></div></AppShell>; }