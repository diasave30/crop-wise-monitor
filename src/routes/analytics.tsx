import { createFileRoute } from "@tanstack/react-router";
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { AppShell, Breadcrumb, PageHeading } from "@/components/layout/app-shell";
import { TrendChart } from "@/components/dashboard/trend-chart";
import { healthTrend, zones } from "@/data/mock-data";

export const Route = createFileRoute("/analytics")({ head: () => ({ meta: [{ title: "Analytics — Ceres Intelligence" }, { name: "description", content: "Review crop health, water stress, and zone comparison analytics." }, { property: "og:title", content: "Analytics — Ceres Intelligence" }, { property: "og:description", content: "Review crop health, water stress, and zone comparison analytics." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: AnalyticsPage });

function AnalyticsPage() { const stress = [{ name: "Low", value: 9, color: "var(--color-primary)" }, { name: "Moderate", value: 2, color: "var(--color-chart-2)" }, { name: "High", value: 1, color: "var(--color-destructive)" }]; const comparison = zones.slice(0, 8).map((zone) => ({ name: zone.id, health: zone.healthScore, moisture: zone.soilMoisture })); return <AppShell title="Analytics"><Breadcrumb label="Analytics" /><PageHeading title="Analytics" description="A focused view of the measurements behind current field decisions." /><div className="grid gap-6 xl:grid-cols-2"><section className="panel rounded-lg p-5"><div className="mb-5"><h3 className="font-medium">Crop health over time</h3><p className="mt-1 text-xs text-muted-foreground">Cumulative health index · 30 days</p></div><TrendChart /></section><section className="panel rounded-lg p-5"><div className="mb-5"><h3 className="font-medium">Water stress distribution</h3><p className="mt-1 text-xs text-muted-foreground">Current reading across monitored zones</p></div><div className="flex h-72 items-center justify-center gap-8"><ResponsiveContainer width="55%" height="100%"><PieChart><Pie data={stress} dataKey="value" nameKey="name" innerRadius={60} outerRadius={90} paddingAngle={3}>{stress.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer><div className="space-y-4">{stress.map((item) => <div key={item.name} className="flex items-center gap-3 text-xs"><span className="status-dot" style={{ backgroundColor: item.color }} /><span className="w-16 text-muted-foreground">{item.name}</span><strong>{item.value}</strong></div>)}</div></div></section><section className="panel rounded-lg p-5 xl:col-span-2"><div className="mb-5"><h3 className="font-medium">Zone comparison</h3><p className="mt-1 text-xs text-muted-foreground">Health score by zone with soil moisture context.</p></div><div className="h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={comparison} margin={{ top: 10, right: 10, left: -20, bottom: 0 }}><CartesianGrid stroke="var(--color-border)" vertical={false} /><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{ fill: "var(--color-muted-foreground)", fontSize: 10 }} /><YAxis domain={[0, 100]} axisLine={false} tickLine={false} tick={{ fill: "var(--color-muted-foreground)", fontSize: 10 }} /><Tooltip contentStyle={{ border: "1px solid var(--color-border)", borderRadius: "6px", background: "var(--color-card)", fontSize: "12px" }} /><Bar dataKey="health" fill="var(--color-primary)" radius={[3, 3, 0, 0]} /><Bar dataKey="moisture" fill="var(--color-chart-2)" radius={[3, 3, 0, 0]} /></BarChart></ResponsiveContainer></div></section></div><p className="mt-5 text-[11px] text-muted-foreground">Data window: August 01–30, 2026 · {healthTrend.length} health observations</p></AppShell>; }