import { Link } from "@tanstack/react-router";
import { ChevronRight } from "lucide-react";
import { PriorityBadge, StatusBadge } from "@/components/shared/status";
import type { Zone } from "@/types/agriculture";

export function ZoneTable({ zones }: { zones: Zone[] }) { return <div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left"><thead className="border-b border-border bg-muted/50"><tr>{["Zone", "Status", "Health score", "NDVI", "Soil moisture", "Priority", ""].map((heading) => <th key={heading} className="px-5 py-3 text-[10px] font-medium uppercase tracking-[0.14em] text-muted-foreground">{heading}</th>)}</tr></thead><tbody className="divide-y divide-border">{zones.map((zone) => <tr key={zone.id} className="transition-colors hover:bg-muted/35"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="font-mono text-xs text-muted-foreground">{zone.id}</span><div><p className="text-sm font-medium">{zone.name}</p><p className="text-xs text-muted-foreground">{zone.crop}</p></div></div></td><td className="px-5 py-4"><StatusBadge status={zone.status} /></td><td className="px-5 py-4 text-sm font-medium">{zone.healthScore}%</td><td className="px-5 py-4 font-mono text-xs">{zone.ndvi.toFixed(2)}</td><td className="px-5 py-4 text-sm">{zone.soilMoisture}%</td><td className="px-5 py-4"><PriorityBadge priority={zone.irrigationPriority} /></td><td className="px-5 py-4"><Link to="/farms/$farmId" params={{ farmId: "green-valley" }} className="grid size-8 place-items-center rounded-md text-muted-foreground hover:bg-muted hover:text-foreground" aria-label={`Open ${zone.id}`}><ChevronRight className="size-4" /></Link></td></tr>)}</tbody></table></div>; }